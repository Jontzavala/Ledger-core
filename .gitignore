# Build output
target/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Local environment / secrets
.env

# Personal notes and Claude instructions (kept local)
# docs/* rather than docs/ so the ADRs below can be re-included;
# git cannot un-ignore a path whose parent directory is itself ignored.
docs/*
!docs/adr/
CLAUDE.md
